
int
void n

{
    

    *li = head;
}

void *parsing(.....)
{
    char *line;

    while(get_next_line(fd, &line))
        if (is_valid(line, data))
            arrayfuncttion[identifier_line(line)](&li, data);
    return(li);
}